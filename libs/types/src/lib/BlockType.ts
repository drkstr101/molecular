/**
 * Contains a set of all valid block (ui component)
 * names
 */
export enum BlockType {
  Button = 'Button',
  Card = 'Card',
  CardGridSection = 'CardGridSection',
  HeroSection = 'HeroSection'
}
